# 📊 막대그래프 대모험 개편 완료 보고서 (초등학교 교육용 자료 개발 프로젝트)

초등학교 4학년 1학기 [막대그래프] 단원의 교육 목적에 완벽하게 부합하도록 1~6단계 전반에 걸쳐 어려운 어휘를 순화하고 시각적 레이아웃과 캐시 문제를 보완하였습니다.

---

## 1. 생성 및 수정한 파일과 작업 내용

* **[app.js](file:///C:/Users/user/.gemini/antigravity/scratch/bar-graph-adventure/app.js):** 
  - **4-1단계 (명소 선택 및 표 완성):** 최소 4개 ~ 최대 7개의 포천 명소 카드를 골라 체크하도록 제어(8개 이상 체크 차단). 선택 완료 시 표(Tbody)를 동적으로 생성하고, 개별 자습용 미니 가상 투표판(녹색 스티커 애니메이션 적용)을 폴백 렌더링합니다. 입력한 수치와 입력한 합계가 수학적으로 일치하는지 검증하는 하이브리드 벨리데이터를 적용해 실시간 교사용 투표판(`survey.html`) 및 가상 투표판 데이터 모두를 지원합니다. **(업데이트: 교사용 피드백 반영을 위해 POCHEON_SPOTS 데이터에서 '자연환경'/'인문환경' 분류를 완전히 배제하고, 동적 표 생성 시 '구분' 열을 삭제하여 2열 구조로 통일하였습니다.)**
  - **4-2단계 (막대그래프 그리기 도구):** 시작 버튼 조작, 행 동적 추가/삭제, 표 입력 잠금(`disabled`), 그래프 렌더링 전환 로직을 구현했습니다. 툴바의 눈금 크기 변경(1/2/5표), 단위 변경(표/명 등), 가로/세로 방향 토글, 교과서 모드(점선 모아레 격자선 추가)를 지원하며 Y축 최댓값 동적 조절(`Math.ceil(maxVal / scale) * scale`)과 눈금선 사이 중간 수치 및 홀수 표의 높이를 정확히 그리는 렌더링 엔진을 탑재했습니다.
  - **4-3단계 (그래프 해석 퀴즈):** 최댓값을 가진 명소를 동적으로 추출하여 Q1 정답을 매칭하고, 가로세로 전환 시 축 변환 성질(Q2) 및 고유 수량 불변 성질(Q3)을 확인하는 퀴즈 채점을 결합했습니다. 통과 시 5단계를 잠금 해제합니다.
  - **5단계 (왜곡 그래프 퀴즈):** 동일 수치 데이터를 눈금 한 칸의 크기에 따라 다르게 보게 만드는 왜곡 현상을 해결하는 눈금 탐구 퀴즈를 채점하고 정답(`b` - 더 짧아지고 완만해진다) 제출 시 6단계를 잠금 해제합니다.
  - **6단계 (배움 정리 및 마스터 수료증):** 4단계에서 저장한 그래프의 방향과 데이터 그대로를 재현하여 완성 그래프 쇼케이스를 렌더링합니다. '오늘의 알짜정리' 3가지 핵심 배움 요소를 클릭해 체크마크(`✔️`)를 모두 채워야만 최종 `#btn-complete-takeaway`가 활성화되며, 클릭 시 마스터 수료증 카드(미니 포트폴리오 그래프 동적 생성)를 띄워 줍니다. window.print()를 활용한 수료증 단독 인쇄 기능 및 로컬 스토리지 초기화와 함께 처음부터 다시 시작하는 초기화 리스너를 바인딩했습니다.
* **[survey.html](file:///C:/Users/user/.gemini/antigravity/scratch/bar-graph-adventure/survey.html):** 
  - 교사 중앙 화면 전용 standalone 실시간 투표판 페이지를 신규 제작했습니다. 8곳의 포천 명소 카드 클릭 시 green sticker가 임의의 각도와 위치로 화면 칠판에 붙으며, Web Audio API를 활용해 별도 오디오 자산 파일 없이 경쾌한 "pop" 사운드를 합성 출력합니다. '숫자 가리기' 토글을 켜면 명소별 스티커 표 수가 물음표(`?`)로 숨겨져 학생들이 실시간으로 직접 세어보는 수학적 활동을 유도합니다. **(업데이트: 교사용 화면 배지 제거, 총 투표 수 레이블 위치 조정 외에 각 명소와 워터마크에 포함되어 있던 '자연환경'/'인문환경' 명칭 및 분류를 완전히 삭제하였습니다.)**
* **[dev.html](file:///C:/Users/user/.gemini/antigravity/scratch/bar-graph-adventure/dev.html) & [index.html](file:///C:/Users/user/.gemini/antigravity/scratch/bar-graph-adventure/index.html):**
  - **사용자 인터페이스(UI) 내 분류 텍스트 제거:** 학생들이 학습하는 데 방해가 될 수 있는 '자연환경/인문환경' 관련 서술(예: 8곳 중 자연환경 4곳, 인문환경 4곳 등)과 분류 카드 배지를 완전히 제거했습니다.
  - **dev.html 파일 동기화:** 수정된 `index.html` 구조를 바탕으로 개발자 테스트 위젯이 결합된 `dev.html`을 UTF-8 인코딩으로 재생성하여 Mojibake(깨진 문자) 없이 동일하게 동기화하였습니다.

---

## 2. 4-6단계 핵심 검증 시나리오 및 확인 방법

### 4-1단계: 항목 선택 및 표 완성
1. **체크박스 제약:** 명소 카드를 클릭하여 4~7개 범위 밖으로 선택 완료 시 경고창이 뜨며, 최대 7개를 넘어갈 수 없게 제한됩니다.
2. **동적 표와 투표판:** 선택 완료 시 하단에 표가 열리며, '가상 투표판 열기' 클릭 시 각 카드별 디폴트 투표 수(예: 산정호수 12표)만큼 녹색 스티커가 표시되는 칠판이 렌더링됩니다.
3. **수학적 검증:** 각 표에 명소별 스티커 수와 합계를 적고 '표 완성하기!'를 누르면 수학적 합산 검증이 진행됩니다.

### 4-2단계: 막대그래프 그리기 도구
1. **직접 막대 그리기 UI:** 4-2단계로 넘어가면 좌측에 4-1단계의 결과 표가 **참고 표**로 제공되며, 우측의 그래프 판은 처음에 모든 막대 높이가 **0인 상태(빈 격자)**로 표시됩니다.
2. **조작 및 그리기:** 각 막대 위에 위치한 **`[+]`와 `[-]` 버튼**을 누르면 막대의 높이가 위아래로 조절되며 현재 값이 막대 위에 숫자로 표시됩니다. 학생이 직접 표 수치와 일치하도록 막대를 그립니다.
3. **도구 컨트롤 활용:** 눈금 간격(1/2/5표 간격 변경), 단위 입력, 가로/세로 그리기 방향을 바꾸어가며 그래프가 가로/세로 방향에 맞게 정상적으로 조작·반응하는지 테스트합니다. (가로 방향 전환 시 `[+]`, `[-]` 버튼이 막대 오른쪽에 자연스럽게 나열됩니다.)
4. **검사 및 저장:** `[그래프 검사하고 저장하기! 💾]` 버튼을 클릭하면 학생이 그린 막대들의 높이가 좌측 결과 표의 데이터와 정확히 일치하는지 자동으로 검증합니다. 일치하면 성공 피드백과 함께 다음 단계로 가는 버튼이 활성화됩니다. 일치하지 않으면 오답 경고 메시지가 발생합니다.

### 4-3단계: 그래프 읽기 퀴즈
1. **동적 보기 로드:** Q1의 보기가 앞서 4-1단계에서 선택했던 명소들로 구성되어 있는지 확인합니다.
2. **해석 채점:** 가장 표가 높은 명소(Q1), `가로축과 세로축이 바뀐다` (Q2), `막대의 길이와 수량` (Q3)을 정답으로 체크하고 제출하면 폭죽과 함께 5단계 잠금이 해제됩니다.

### 5단계: 생활 속 그래프 탐구 (눈금 크기 비교)
1. 동일한 캔 수량(10, 5, 15개)을 표현할 때 눈금 크기(1개 vs 5개)에 따른 막대 높이 차이를 시각적으로 확인합니다.
2. 눈금이 커질수록 막대 높이가 `더 짧아진다 (완만해진다)`를 정답으로 제출하면 폭죽과 함께 6단계 잠금이 해제됩니다.

### 6단계: 알짜정리 배움 정리 및 마스터 수료증
1. **그래프 쇼케이스:** 4단계에서 세로 방향으로 그렸다면 세로 그래프가, 가로 방향으로 저장했다면 가로 그래프가 쇼케이스에 동일 수치로 렌더링됩니다.
2. **알짜 배움 체크:** 1, 2, 3번 배움 카드를 터치하여 취소선และ 체크표시가 3개 모두 채워져야만 수료증 발급 버튼이 활성화됩니다.
3. **마스터 수료증 발급:** 버튼 클릭 시 수료증 영역이 부드럽게 나타나며 탐험가 이름, 작성일자, 내가 만든 그래프의 미니 바 차트가 수료증 본문 중앙에 미니 포트폴리오로 삽입됩니다.
4. **인쇄 및 다시 하기:** 'PDF 저장/인쇄' 클릭 시 프린트 미리보기 창에 수료증 카만 단독 렌더링되며, '다시 도전'을 클릭하면 전체 세션 데이터가 깔끔하게 리셋됩니다.

---

## 3. 가로 그래프 전환 시 레이아웃 잘림 및 조작 버튼 이동 문제 해결

* **문제 상황:**
  - **왼쪽 레이블 잘림**: 막대그래프 그리기 도구(시뮬레이터)에서 그래프 방향을 '가로'로 바꿨을 때, Y축의 명소 이름(레이블)들이 절대 위치(`left: -90px; width: 85px;`)로 배치되나 부모 격자 컨테이너(`.ebs-chart-container`)의 왼쪽 여백(`margin-left`)이 세로 그래프 기본값인 `35px`로 고정되어 있어 화면을 뚫고 잘려 보이는 시각적 오류가 발생했습니다. 또한, 글자 수가 긴 항목은 좁은 너비 때문에 강제로 줄바꿈이 일어나 레이아웃이 겹쳤습니다.
  - **조작 버튼 위치 흔들림**: 가로 모드에서 막대 개별 조작 `[+]`, `[-]` 버튼이 막대 오른쪽에 상대 정렬(flex)로 매핑되어, 막대 수치가 바뀌어 가로 길이가 늘어나거나 줄어들 때마다 버튼이 실시간으로 좌우로 계속 이동했습니다. 이로 인해 학생이 연속해서 클릭해 값을 조절할 때 매번 마우스 커서 위치를 따라 움직여야 하는 치명적인 사용성 불편이 존재했습니다.

* **수정 조치:**
  - **격자 컨테이너 여백 확장 (`style.css`)**: 가로 그래프일 때 격자의 왼쪽 여백(`margin-left`)을 **`140px`**, 오른쪽 여백(`margin-right`)을 **`70px`**로 늘려 안전한 레이블 공간과 고정 버튼 배치 공간을 한 번에 확보했습니다.
  - **레이블 영역 크기 확대 및 말줄임표 처리 (`style.css`, `app.js`)**: 레이블 상자의 너비를 `85px`에서 **`130px`**로 넓혀 긴 명소 이름도 원활하게 표시하며, 텍스트가 극도로 길어질 경우 자동 말줄임표(`...`) 처리와 마우스 오버 툴팁(`title` 속성)을 제공하여 시각적 완성도와 탐구성을 모두 높였습니다.
  - **조작 버튼 위치 고정 및 absolute 정렬 (`app.js`, `style.css`)**: 가로 모드 조작 버튼(`ebs-bar-adjust-horizontal`)의 정렬 방식을 상대 정렬에서 `position: absolute; right: -60px;` 형태로 수정하였습니다. 이를 통해 격자선 오른쪽 경계의 고정된 위치에 버튼이 안전하게 안착하여, 막대 길이가 어떻게 변하더라도 버튼이 흔들리지 않고 제자리에서 쾌적하게 조작할 수 있도록 개선했습니다.
  - **전체 단계 동기화 (`app.js`, `style.css`)**: 4-2단계 조작 영역뿐 아니라, 4-3단계 해석 퀴즈용 그래프 프리뷰 및 6단계 최종 완성 그래프 쇼케이스 영역 전체에도 일련의 여백 크기 및 레이아웃을 동기화하여 통일감을 유지했습니다.

---

## 4. 활동 4-3 퀴즈 카드 내 실시간 막대그래프 프리뷰 및 교육적 시각화 개선

* **문제 상황:**
  - 활동 4-3은 학생이 자신이 직접 완성한 그래프를 보고 해석 퀴즈를 푸는 단계인데, 이전 프리뷰 그래프는 축과 눈금(격자선)이 통째로 생략되어 있어 눈금 한 칸의 수치 대조를 학습하는 교육적 효과가 제한적이었습니다.
  - 또한 막대 위에 수치가 텍스트로 바로 표시되어 있어, 학생들이 수량 축의 눈금을 읽지 않고도 직관적으로 퀴즈 정답을 맞출 수 있는 우회 가능성이 있었습니다.
  - **선택지 내 득표수 노출**: Q1 객관식 보기의 각 명소 이름 우측에 `(10표)`와 같은 득표수가 명시되어 있어, 그래프를 보지 않고 텍스트 득표수 크기만 비교해도 정답을 고를 수 있어 그래프 해석이라는 교육적 효과가 없었습니다.
  - **축 용어의 미순화**: 초등학교 4학년 수준에 생소할 수 있는 '축', '가로축', '세로축'이라는 수학적 용어가 학생 화면에 그대로 노출되어 질문의 이해도를 다소 낮추는 문제가 있었습니다.

* **수정 조치:**
  - **눈금 및 축선 온전한 재현 (`app.js`, `style.css`)**: 4-2단계 저장 시 사용했던 눈금 크기(scale) 정보를 동적으로 보존하여, 4-3 프리뷰 그래프 렌더링 시 **자신이 그린 그래프 격자선과 수량 축(X/Y축) 및 눈금 수치 레이블**을 온전하게 재현하도록 개선했습니다.
  - **막대 위 수치 레이블(숫자) 제거 (`app.js`)**: 퀴즈를 해결할 때 막대 위의 숫자를 보고 정답을 찍는 대신, 그래프의 수량 축과 격자 눈금을 직접 세어보며 분석하도록 **막대 위 숫자 렌더링을 완전히 제거**하였습니다.
  - **그래프 동적 가운데 정렬 (`app.js`)**: 눈금과 축선이 재현되면서 그래프 격자 부분이 왼쪽 레이블 영역(`130px`)과 오른쪽 여백 영역(`70px`)의 너비 편차로 인해 쏠리지 않도록, `margin: 0 auto;`와 `translateX` 변환(Transform) 식을 결합해 전체 차트 구성이 화면 정중앙에 배치되도록 밸런싱을 맞췄습니다.
  - **선택지 득표 수 표기 삭제 (`app.js`)**: Q1 객관식 보기에서 명소명 우측의 득표수 텍스트(`(${item.val}${state.customGraph.unit})`)를 완전히 제거해, 오직 그래프 눈금 정보만으로 정답을 풀도록 강제했습니다.
  - **초등 교육과정 용어 순화 (`index.html`, `dev.html`, `app.js`)**: 초등학교 4학년 수준에 생소할 수 있는 '축', '가로축', '세로축'이라는 표현을 '가로와 세로' 또는 '가로', '세로'로 전부 순화하고, 4-3단계 Q2의 질문 및 TTS(음성) 안내, 채점 오답 피드백 텍스트까지 일괄 변경하여 학생들의 직관적인 용어 이해를 도왔습니다.

### 12. 활동 4-3: 가로 라벨 겹침 및 이모티콘 제거 보완 (v1.1.6)
* **그래프 가로/세로 라벨 이모티콘 완전 제거 (`stripEmoji` 적용)**:
  - 기존 정규식 필터가 일부 기기/환경에서 이모티콘이나 특수 기호를 완전히 걸러내지 못해 텍스트 뒤의 이모티콘이 줄바꿈(Word Wrap)되는 현상이 발생하였습니다.
  - 이를 근본적으로 해결하기 위해, 한글, 영어, 숫자, 괄호, 공백 문자만을 남기고 모든 이모티콘과 특수문자를 완벽히 제거하는 안전한 정규식 필터(`stripEmoji`)를 적용하였습니다.
  - 이 필터는 활동 4-2, 활동 4-3 프리뷰 그래프, 6단계 쇼케이스 그래프, 수료증 미니 그래프의 모든 가로/세로 라벨에 적용되어 텍스트가 강제 줄바꿈되어 겹치는 현상을 완전히 방지합니다.
* **Q4-3 프리뷰 그래프 하단 여백 확보**:
  - 그래프 하단 가로 라벨과 아래에 위치한 '방향 바꾸기' 버튼의 겹침 현상을 해결하기 위해, 활동 4-3 그래프 컨테이너의 bottom margin을 `55px`에서 `75px`로 대폭 확장하였습니다.
  - 절대 배치된 가로 라벨 영역(bottom: -25px)과 하단 버튼 사이에 넉넉한 50px 크기의 버퍼 공간이 생겨, 어떠한 화면 크기나 브라우저 환경에서도 요소들이 서로 겹치지 않고 정렬되도록 개선했습니다.
* **캐시 버스팅 및 BOM 정합성 복원**:
  - `index.html` 및 `dev.html` 파일의 버전 파라미터를 `v=1.1.6`으로 업데이트하여 최신 변경사항이 브라우저 캐시를 뚫고 정상 로드되도록 조치했습니다.
  - 파일 편집 후 유실된 HTML 파일들의 UTF-8 BOM 시그니처(`EF BB BF`)를 .NET 파일 스트림 방식으로 복원하여, 태블릿 환경에서의 문자 깨짐(Mojibake) 오류를 철저히 방어하였습니다.
